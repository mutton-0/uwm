# 预注册与修正案账本

> 纪律：对 held-out 集的每次计算**先登记后执行**。本文件的登记时间戳早于对应结果文件的写入时间。

---

## PV｜预测-验证收尾（登记于跑数之前）

### PV.0 两处必须先声明的设计偏离

工单原文有两条前提与实际数据不符，**登记为偏离，不静默修改**：

**偏离 1 —— 「A 类大集」不存在。**
n1_d2 全量挖掘的 A 类共 **291** 个事件，T2.5 小集已采集 **288** 个（99.0%）。
不存在一个更大的 A 类集合可供外推验证。
→ **改为两块验证集**（见 PV.1），A 类的复现放到**另一轮独立挖掘**（tier_m，A=312，
   clean/ghost 窗口与判据不同，CoT 文本从未采过）上做**准独立复现**。

**偏离 2 —— P(b<0) 在 tier_m 上已被观测过（0.511，见 `safety_metrics.json`）。**
拿它当"预测"不构成盲测。
→ 预测② 改到**完全 held-out** 的 n1_d2 B/C 类上评估：这 657 个事件
   **既没进过 G2 缓存（b 未知）、也没采过 CoT 文本（语言未知）**。

**偏离 3 —— 预测③ 的前提在小集上不成立。**
工单写"（若小集有此现象）"。小集实测：提及子集达标率 0.356 vs 未提及 0.306，
**差值 +0.050，Fisher p=0.53，不显著**，结论是「看见也不刹」。
→ 因此登记的是**结构复现 = 仍无显著跳升**。若大集反而出现显著跳升，
   则说明小集 n=59 功效不足被暴露，处方级结论须改写为「看见就会刹」——**两种结局都可报**。

### PV.1 验证集（跑数前冻结）

| 集 | 内容 | n | b 是否已知 | 语言是否已知 | 用途 |
|---|---|---|---|---|---|
| **V-A** | tier_m 的 A 类（独立挖掘轮） | 312 | 已知（缓存内） | **未知** | 预测① 的准独立复现；③ 的部分检验 |
| **V-BC** | n1_d2 的 B + C 类 | 657 | **未知**（未进 cache_needed） | **未知** | 预测②③ 的完全盲测；① 的跨类型外推 |

B = 横向切入，C = TTC 突降。两集都需新前向；V-BC 还需补 G2 缓存才有 b。

### PV.2 三条预测（**冻结，跑完不许调口径**）

| # | 预测 | 判定阈值 | 评估集 | 小集参照 |
|---|---|---|---|---|
| **P1** | A 类语言行人提及率 **< 30%** | 点估计 < 0.30 | V-A（主）；V-BC 报跨类型值 | 小集 0.205 [0.142, 0.267] |
| **P2** | b 符号对半分：**P(b<0) ∈ [45%, 55%]** | 点估计落在区间内 | V-BC（完全盲测） | tier_m 正例 0.511（已观测，故不用作盲测） |
| **P3** | 提及×刹车 2×2 **结构复现 = 仍无显著跳升** | 提及 vs 未提及达标率差的 Fisher p ≥ 0.05 | V-A + V-BC | 小集 +0.050，p=0.53 |

**判定规则**：三条全中 ⇒ 在 `final_report.md` 补「预测-验证」一节，
称小集诊断成功预测大集失效模式，demo 的外推主张获得端到端确认；
任一不中 ⇒ 如实记录失配形态，并标注诊断的适用边界。**不得事后调整阈值或换评估集。**

### PV.3 附带分层（探索性，不参与判定）

- 日 / 夜（`is_night`）；
- bbox 成像面积分箱（仅 n1_d2 有 `area_px`；tier_m 那轮挖掘未存，改用 `d_long` 作代理并注明）。

分层结果一律标注**探索性**，用于看签名是否跨条件稳定，**不进三条预测的判定**。

### PV.2b 新增登记：提及×刹车的**几何控制**口径（跑数前补登）

**动机**：「提及」与目标成像几何强相关 —— 小集实测 提及子集的成像面积中位 5672 vs 未提及 2932 px
（MWU p=1.1e-5）、纵向距离 21.5 vs 28.7 m（p=1.1e-6）。而**大目标本身也更容易因纯几何原因触发减速**。
因此 2×2 的任何跳升都可能是**几何在两头同时使劲**，而非"感知门控了行为"。

**登记口径（冻结）**：
1. 主检验：提及 vs 未提及的达标率差（Fisher）——同 P3；
2. **控几何后重检**：以 `log10(area_px)` 与 `ecc` 为协变量做 logistic
   （因变量 = b 达标；自变量 = 提及 + 两个几何协变量），看**提及项的系数**是否仍显著；
   并列做**几何分层**版本（面积三分位内分别算差值，复用 Q3 的 caliper 机器）；
3. **读法（冻结）**：
   - 控几何后关联**仍显著** ⇒ 可读作 **「感知门控行为」**；
   - 控完**消失** ⇒ 读作 **「几何共因」**，不得再宣称感知门控。

**适用范围**：几何协变量只有 n1_d2 那轮挖掘存了（`area_px` / `ecc`），
**V-A（tier_m）无几何字段，故几何控制只在 V-BC 上做**；V-A 只报未控版本并注明。

### PV.2c 两种结局的处方含义（**跑数前写死，不许事后挑**）

| 大集结果 | 读作 | 病灶 | post-train 处方 |
|---|---|---|---|
| **仍无显著跳升**（且功效充足） | 「看见也不刹」 | **双病灶**：感知丢失 + 看见后耦合弱 | 感知与耦合**都要修** |
| **跳升且控几何后仍在** | 「看见就会刹，只是看不见」 | **单病灶**：锁死在感知 | 只修感知，处方最干净 |
| 跳升但控几何后消失 | 「几何共因」 | 不构成感知门控证据 | 维持双病灶判断，另查几何混淆 |

功效说明：若大集提及子集 n 仍不足以排除中等幅度跳升（CI 半宽 > 0.10），
按三态纪律报 **「不可估」**，不得读作"仍无跳升"。

### PV/P.A11 工单 P 的口径对齐修正（跑数中途登记，两版并列报告）

**问题**：首轮 P1 把 Alpamayo 的 `ego_history`（自车运动史）**各条件各取各的**。
但 clean 与 ghost 相隔 ~1.5 s，自车运动史本就不同，b 里会混进
「这两帧之间自车快了还是慢了」——**正是 SimLingo 侧已经修过的同一个混淆**
（commit 5c1366d：把 prompt 的 `Current speed` 锚定到 clean 帧，使两条件唯一差异是图像）。

**修正**：新增 `--ego-anchor clean`，把 `ego_history` 锚定到 clean 帧，图像仍各取各的。
这与 SimLingo 受到的处理**同构**，不是「为阳性对照换更友好的设定」——
它移除的是一个已知混淆，且方向不预设有利于通过。

**报告纪律**：`clean` 锚定版为**主读数**（与 SimLingo 同口径），`per_frame` 版
作**敏感性**并列报告。两版数字都落盘（`p1_alpamayo_{clean,per_frame}.json`），
不得只报其中一个。

**登记时点声明**：`per_frame` 版的结果（b-AUC 0.439）在本条修正案登记**之前**已被看到，
因此本条不是盲测。之所以仍然执行，是因为口径不一致本身是硬伤（同口径是 guide 附录 D
的明文纪律），与结果好坏无关。

---

## A 系｜方法学修正案（跑数过程中产生，已在各报告内记录）

| # | 修正 | 原因 | 影响 |
|---|---|---|---|
| A1 | S1 判据③ 从"500 集落在 2k 带内"改为**统计相容性** | 字面判据有机械偏差：小集误差是 2k 带宽的 2.1 倍 | 0.685 → 0.943，E-S1 由 FAIL 改 PASS |
| A2 | S1 趋势检验 Page's L → **Jonckheere–Terpstra** | Page's L 要求完全区组，实测完全区组 scene <5 | Page 记"不可估" |
| A3 | T2 随机对照改为**整条 ±α 阶梯比斜率**，逐层自带零分布 | 单点对照分不开"无符号扰动"与"有符号方向效应" | E2 由 FAIL 改**不可估** |
| A4 | T1-L selectivity 统计量 EVR₁ → **配对一致性 r + held-out 投影** | 不去均值 SVD 对行符号翻转不变 ⇒ EVR₁ 置换零分布恒等于观测 | 置换检验从无效变有效 |
| A5 | T1-L 的 PCA **不去均值** | δ 的均值方向就是概念方向 | 轴可提出 |
| A6 | `load_cache` 加 schema/账本双过滤 | cache 目录 647 个孤儿 h5 会使脚本崩溃 | 管线可跑 |
| A7 | W3 行为端对标值 guide 写 0.555，**实测 0.534（p=0.157）** | 重算所得 | guide 的战略前提被削弱 |
| A8 | T2.5 编码器：事件级拼 2 帧导致 35.1% 事件双命中 fast+slow | 实现缺陷 | 补逐帧口径；二值 2×2 的 κ 偏低系稀释所致 |
| A10 | PV.3 分层解读：跨类型比较**按目标类别对齐口径** | V-BC 的目标 95.4% 非 VRU（car 338 / barrier 76 / cone 69…），VRU 词表在其上量的不是同一件事 | 「跨类型提及率更低」与「几何签名未复现」两条解读**作废**；同口径下三集一致 0.205/0.205/0.167；几何签名状态改为**未检验** |
| A9 | 安全学分层与 S1 曲线**符号冲突**（+0.075 vs JT −2.155，p 均≈0.03） | 人群不同：S1 被负例主导，安全表只用正例 | 安全结论以正例为准；E-S1 不适用于正例子集 |

## 自我更正记录

基于 5 个随机种子曾判定"L22 零分布不居中"。扩到 20 个种子后不成立（均值=0 检验 p=0.12）。
正确结论是"对照功效不足"，非"对照有偏"。见 `t1l_t1q_report.md` §4.4。

---

# 第二轮预注册：方向效度验证（DV）+ G/F/I/C 四轴证明（FA）

> 登记时间：2026-08-29，**早于本轮任何新结果文件的写入**。
> 依据工单：`docs/direction_vector_discovery_validation_plan.md`、`docs/four_axis_proof_experiment_workorder.md`、
> `docs/g_axis_positive_calibration_diffusiondrive.md`、`docs/vla_base_model_selection_protocol.md`。
> 轴名一律使用 2026-08-29 定稿的 **G/F/I/C/D**，旧 R/S 记号的映射见 `axis_naming_alignment.md`。

## DV.0 单一主读数登记（每个阶段一条，其余全部标为敏感性分析）

| 阶段 | **预注册主读数** | 判定门槛 | 敏感性分析（不参与判定） |
|---|---|---|---|
| A1 权重投影 | 候选方向的概念得分**超出同规模随机方向池 max 极值零分布的 95 分位** | 超出者才进入候选清单 | 不同 top-k、不同概念子表 |
| B1 $v_{hazard}^{clean}$ | region_mean × (A vs D2a) 的 **4 折 CV-AUC − D2cV 证伪地板** | > +0.05 判 PASS；CI 含 0 判不可估 | 其余 7 种池化口径、PCA 臂 |
| B1-carla $v_{hazard}^{carla}$ | region_mean × (Hcar vs Ncar) 的 **CV-AUC − 随机方向地板** | 同上 | vision_mean / last_token |
| C 交叉印证 | A1 候选 × B 方向的 **max 余弦是否超出极值零分布** | 超出 ⇒ 双重印证 | 逐层余弦剖面 |
| D 操纵 | ±α 阶梯**整条斜率**对比同层 ≥20 seed 随机零分布 | 见 A3 修正案 | 单点 α、不同 token 集 |
| D 解析 | 解析预测 Δv_plan 与实测斜率的**逐事件相关 r** | 符号一致且 r>0 ⇒ H3 成立 | 二次项残差 |
| E 效度 | 两条红线（跨分布 held-out、强 ground truth）**逐方向是否通过** | 三态 | — |

## FA.0 四轴子实验主读数登记

| 任务 | **预注册主读数** | 三态判据 |
|---|---|---|
| **T-G** | DiffusionDrive 在**同一刺激集**上的 CV-AUC(A vs D2a) **减去自身 D2cV 证伪地板** | 显著 >0 ⇒ H1（方法有效，SimLingo 的 FAIL 是标本属性）；落地板 ⇒ H-artifact；CI 含 0 ⇒ 不可估 |
| **T-F** | $\cos(v_{brake}^{obs}, v_{brake})$ 对比 ≥20 个随机方向的余弦零分布 | 显著高于零分布 ⇒ PASS；不显著但同号 ⇒ 不可估；反号或落在零分布内 ⇒ FAIL |
| **T-I** | 两模型的 $I_m = 1 - D_{L^*}$ 排序 + 干涉角 $|\theta_{L^*}|=|\cos(v_{domain},v_{hazard})|$ | 单模型可行时降级为探索性读数，不做跨模型排序 |
| **T-C** | $C_m$ = top-2 层 recovery 占比 / 全层 recovery 之和（逐场景算再汇总） | 报点估计 + scene 级 bootstrap CI；t-SNE 明确标注为相关性证据 |

## FA.1 设计偏离登记（执行前已知，不静默处理）

**偏离 1 —— T-I 的 3DGS 反事实场景集（P3）在本仓库内不存在。**
全盘检索未发现 3DGS 重建资产（`/data/zihao/HUGSIM` 只有渲染器代码，无本项目场景）。
**替代方案**：使用 `ghosthead_v1` 已有的**同几何双渲染配对**——
`transfered` = CARLA 引擎渲染（sim）、`origin` = 世界模型生成的真实感重绘（real），
同一场景同一构图、唯一变量是渲染风格，**在因果结构上与 P1「域配对 real↔3dgs」等价**
（都是 do(appearance)，几何/actor/ego-GT 全部锁死）。
24 场景 × 3 变体 = 72 个 scene-variant，DiffusionDrive 侧已完成推理。
**代价与声明**：这不是 3DGS 重建，因此 I 轴结论的适用范围写作"CARLA 渲染 ↔ 世界模型真实感渲染"，
而非"sim ↔ real"；工单 §4 要求的"3DGS 渲染风格相对各自训练域的距离"改述为"世界模型真实感渲染"。
最终结论以麦迪逊阶段的 real↔3DGS 版本为准（协议 §3.5 已有同样的降级声明）。

**偏离 2 —— T-F 的回归目标"真实 a_brake"在事件账本里没有现成字段。**
`events_all.jsonl` 只存了逐帧 `ego_speed_mps`（clean/ghost 各 2 帧）。
**处理**：回到 nuScenes ego_pose 重算每个事件在 $[t_{emergence}, t_{emergence}+3\,\mathrm{s}]$ 窗口内的
真实自车纵向加速度序列，取该窗口最小值（最强减速）为 $a_{brake}$（单位 m/s²，负值为减速）。
这是**人类驾驶员的真实行为**，构成 Stage E 红线 2 要求的强 ground truth，而不是模型自身输出的代理量。
以 TTC 为回归目标的版本作为敏感性分析并列报告（工单 §3 明文允许）。

**偏离 3 —— T-G 的 DiffusionDrive 没有"层"概念上的 LLM decoder。**
DiffusionDrive 的可读位置是 TransFuser 编码器的 8 个 `encoder_selfatt` 输出（320 token = 256 图像 + 64 BEV latent）。
按协议 §4 规则 3「层按角色对齐、不按层号」，把这 8 层视作该模型的可读层集合，
池化口径取「图像 token 均值」= SimLingo `vision_mean` 的同构物、「bbox 内图像 token 均值」= `region_mean` 的同构物。
跨模型比较只比**模型内归一化后的读数**（AUC、与自身地板之差），不比原始激活量。

## DV/A12 峰层选择规则修正(跑数中途,已对两条臂同步应用)

**问题**:原规则"取 4 折各自嵌套选层 argmax 的**众数**"在平局时退化为"取层号最小者"。
CARLA 臂实测峰层为 `[8, 8, 0, 0]`,2-2 平局 ⇒ 规则给出 L*=0(第 0 层,几乎不可能是概念峰层)。

**修正**:改为**逐折 S_sel 层剖面(AUC-by-layer)的均值取 argmax**。该规则无平局退化,
且用满了每折的全部层信息而非只用 argmax 一个点。**对 nuScenes 与 CARLA 两条臂同步应用**,
不是只在结果不好看的那条臂上改。修正后:CARLA L*=8(与众数 8/8 一致),nuScenes L*=9(与原众数一致)。
两条臂的众数值一并落盘为 `peak_layer_mode_sensitivity`。

**影响**:只影响冻结方向的读出层(Stage C/D 用),不影响 CV 读数本身(CV 的选层始终在折内嵌套完成)。

## FA/A13 T-G 主池化口径的确认(执行中的一次自我更正)

首版 `g_axis_dd_readout.py` 在 H1 判定处误取 `region_mean` 作主口径。
按 `g_axis_positive_calibration_diffusiondrive.md` §3 表格明文——"至少跑 **vision_mean**
(与 SimLingo 主读数口径一致,便于直接对比)"——且 SimLingo 侧 `n1_report.md` 的主读数
(D2a 0.568 / D2cV 0.574)本就是 vision_mean,**主口径应为 vision_mean**,已更正。

**透明声明**:两个口径的全部数字在同一次运行中同时产出、同时落盘,
且 vision_mean 的主口径地位由工单文档在跑数之前指定,不是看到结果之后挑的。
两个口径**结论不一致**(vision_mean +0.093 CI[0.004,0.174];region_mean −0.051 CI[−0.138,0.037]),
这一不一致在 T-G 报告的 Results 与 Discussion 中并列呈现,不得只报有利的一侧。

## DV/A14 Stage C 多重比较零分布的修正(把命中数从 11 改成 0)

**问题**:首版 Stage C 用 3000 个随机抽样的 value vector 估零分布,再用**高斯尾**
(Bonferroni z·sd)外推阈值。实测该零分布尾部远重于高斯 —— 经验 q99.9 = 0.100~0.154,
而高斯尾给出的阈值只有 0.071~0.084,即**阈值比零分布自己的千分位还低**。
按这个偏松的阈值,190×5=950 个格子里有 **11 条**"双重印证"。

**修正**:改用**全总体枚举**做零分布 —— 24×4864 = 116,736 个 value vector 全部参与
(扣掉 190 条候选本身,余 116,546),每个 unit 与各轴在**自己的层**上算 |cos|。
α = 0.05/950 = 5.3e-5 对应总体第 ~6 名,可直接取经验分位,**不需要任何分布假设**。
按修正后的阈值,双重印证 **0 条**,H2 **不成立**。

**这条修正把一个阳性结果改成了阴性结果**,是本轮唯一一处方向性反转,故单独立条记录。
两版阈值与全部余弦值均落盘于 `cosine_matrix.json`(`gauss_bonferroni_REJECTED` 字段
保留被否决的旧阈值以便复核)。

**方法学教训(将写入 generalizable_tips.md)**:在"N 个候选取 max"的场景里,
用观测量的方差 + 高斯尾外推极值阈值是不安全的——权重空间的余弦分布尾部显著重于高斯;
只要候选总体是可枚举的,就应当直接枚举总体取经验分位,而不是外推。

## DV/A15 Stage C 余弦基的修正(RMSNorm 基 -> 残差流原始基)

**问题**:A1 给 value vector 打概念分时,必须先过最终 RMSNorm 再乘输出嵌入(logit lens 的定义)。
首版 Stage C 沿用了这个 **RMSNorm 后的基**去和数据驱动方向算余弦。
但 `v_hazard_clean`/`v_brake`/`v_domain` 都是在**逐层原始隐状态**上拟合出来的,
两者不在同一个基里,余弦的几何含义不成立。

**修正**:Stage C 主口径改为**残差流原始基**(FFN value vector 未经 RMSNorm 直接单位化 ——
FFN 本来就是把它按系数写进残差流的),RMSNorm 基版本作为敏感性并列(`--basis rmsnorm`,`_rmsnorm` 后缀)。
两个基下 H2 的结论一致(双重印证均为 0 条),故本修正不改变判定,只修正几何依据。

## DV/A16 证伪地板的口径修正(把 T-G 的 H1 判定从"成立"改回"不可估")

**问题**:项目既有口径(`n1_cv.py`,`n1_report.md` 已发表的数字)是——
方向与峰层**只**在 A vs D2a 上拟合/选择,其余负类(D2cV 证伪地板、D2b 上下文…)
用**同一条方向、同一个峰层**投影后报数。首版 `b1_hazard_clean.py` 与
`g_axis_dd_readout.py` 却对**每个负类各自重新拟合了一条方向**。

两者量的不是同一件事:
  * 共享方向(既有口径)= "这条 A-vs-D2a 方向里有多少只是'A vs 任意 VRU'" ← 证伪地板的正确定义;
  * 各自重拟合(首版错误)= "能不能找到一条分开 A 与 D2cV 的方向" ← 系统性偏高,且与 n1_report 不可比。

另有两处连带缺陷一并修正:
  * **折映射覆盖面**:首版折映射只覆盖 POS+NEG 的场景,导致同一方向投影其余负类时
    ~70% 的 D2cV 被静默丢掉(n_neg 212 → 61);现改为覆盖缓存内**全部事件类型**的场景,与 n1_cv 逐字一致。
  * **折分配漂移未量化**:同一读数在不同折分配下可漂 ±0.04~0.08,与效应量同量级。
    现对主读数−地板加报 **10 个 CV seed 的均值±sd 与范围**,作为效度声明的一部分。

**后果(必须明写)**:按修正后的口径,T-G 的 H1 判定由"成立"改回 **不可估** ——
DiffusionDrive 主读数−D2cV 地板 = +0.047,scene 级 bootstrap 95% CI [−0.028, +0.122],跨 0;
10 seed 上为 +0.025 ± 0.025(范围 −0.018 ~ +0.070)。
首版给出的 +0.093 [0.004, 0.174] 是**错误口径**下的数字,已作废,不得引用。

**登记时点声明**:错误口径下的阳性结果(+0.093, CI 不跨 0)在本条修正登记**之前**已被看到。
仍然执行修正,是因为口径不一致本身是硬伤(与 n1_report 的可比性是 T-G 全部意义所在),
与结果好坏无关;且修正方向不利于我们已经写下的结论,不是择优。

## DV/A17 Stage E 红线 1 的泄漏修正（held-out 必须正负两侧都干净）

**问题**：首版红线 1 把"从未参与拟合的**正例类别**"当作 held-out（B、C），
但负例仍用 `D2a` —— 而 `v_hazard_clean` 正是在 (A, D2a) 上做的全量拟合。
负例侧被拟合系统性压低后，"B vs D2a" 的 AUC 会虚高。
直接证据：同一条冻结方向在**它自己的拟合集** A vs D2a 上得到 **AUC = 1.000**
（而其 4 折 CV-AUC 只有 0.547），泄漏幅度一目了然；据此推断 B vs D2a = 0.847、C vs D2a = 0.769
同样被污染。

**修正**：held-out 判定改为**正负两侧都不得出现在该方向的拟合集里**。
干净组合为 正例 ∈ {B, C} × 负例 ∈ {D, D2b}（后两类从未参与任何方向拟合）。
每一行都带 `held_out_clean` 标志；`false` 的行保留在 JSON 里但**只作管线 sanity，不作效度读数**。

**后果**：`v_hazard_clean` 的跨类别 held-out 读数由 0.847/0.769 降为
**0.543 / 0.501（vs D）与 0.533 / 0.496（vs D2b）**，全部落在同层随机方向地板附近。
首版数字作废，不得引用。

## DV/A18 §2.5 泛化性证明的实现方式（零 GPU 成本的等价替代）

计划 §2.5 要求"复用现有 nuScenes 挖掘管线，**只换触发判据**（如'前车急刹'或'无保护左转冲突'）"，
补一次轻量级第二场景类型复现。

**实现**：n1_d2 的事件账本里**已经存在**两条与 `event_A`（VRU 突现）完全不同的触发判据——
`event_B`（近距 cut-in：$d_{long}$ < 25 m、TTC < 6 s、横向速度指向本车道，n = 103）与
`event_C`（通用 TTC 骤降：1 s 窗口内 TTC 掉 ≥ 1.5 s 且掉后 < 5 s，n = 554）。
把正例换成它们、其余口径（负例体系、池化、方向提取、峰层规则、CV、证伪地板）**一律不动**，
即构成计划要求的复现，且**零 GPU 成本**（复用同一批缓存）。

**登记声明**：B/C 两类此前**从未参与**任何方向的拟合或选层（它们同时也是 Stage E 红线 1 的
held-out 正例来源），因此这次复现不是在已用过的数据上重算。结果见
`direction_validity_report.md` §5.3、`b1_v_hazard_b.json`、`b1_v_hazard_c.json`。
**与"前车急刹/无保护左转"不同**：本实现用的是已有判据而非新挖判据，
故它验证的是"同一挖掘管线内换触发条件"的自洽性，未验证"换到完全不同的场景族"——
后者仍待麦迪逊阶段或新一轮挖掘。

## FA/A19 T-G 刺激集不等的修正（把 DiffusionDrive 侧的 D2cV 从 141 补齐到 212）

**问题**：首轮 DiffusionDrive 缓存按 `--types A D2a D2b D2c --use-matched` 生成，
即只缓存了 `matched_D2c` / `matched_D2b` 里的事件。但证伪地板 **D2cV** 的定义是
"`matched_D2cV` ∩ D2c 类 ∩ VRU 类别"，其成员**不全在 `matched_D2c` 里**——
于是 DiffusionDrive 侧只拿到 **141** 条 D2cV，而 SimLingo 侧是 **212** 条。
D2bV 同理：33 vs 78。

**这直接违反 T-G 的立身之本**（`g_axis_positive_calibration_diffusiondrive.md` §3：
"必须用与 SimLingo 完全相同的刺激集"）。地板恰恰是判定所依赖的量，用更少的负例估地板，
两侧不可比。

**修正**：补缓存 116 个缺失事件（0.32 s/event，约 40 s GPU），使两侧的
D2a = 283、D2cV = 212、D2bV = 78 **完全一致**，重跑 T-G 全部读数。

**后果（数字全部变化，方向不变）**：主口径 `vision_mean` 的"主读数 − 自身 D2cV 地板"
由 +0.047 [−0.028, +0.122] 变为 **+0.009 [−0.047, +0.065]**；10 个 CV seed 上由
+0.025 ± 0.025 变为 **+0.012 ± 0.032**。H1 判定**仍为不可估**，但证据比修正前更弱。
更重要的是：修正后**"DiffusionDrive 相对置换零分布高出更多"这一方向性证据也不再稳健**——
`vision_mean` 上 DD +0.065 vs SimLingo +0.021，但 `region_mean` 上 DD +0.024 vs SimLingo +0.056，
**两个口径给出相反的排序**。首版报告里"方向一致于 H1"的表述据此作废，已改写。

**登记时点声明**：修正前的数字已被写进报告初稿。执行修正的理由是刺激集不等属硬伤，
与结果好坏无关；且修正方向同样不利于已写下的结论。

## DV/A20 退化池化守卫（tier_m 上 region_* 恒为 0，AUC 恒等于 0.500 不得当读数）

**问题**：红线 1c 首次跑 tier_m 独立挖掘轮时，`v_hazard_clean` 与 `v_hazard_carla`
都给出 **AUC = 0.500、随机地板 0.500、超出 +0.000** —— 两条完全不同的方向给出逐位相同的数，
本身就是仪器报警。核查发现：tier_m 是更早的挖掘轮，事件账本**没有 `bbox_xyxy` 字段**，
区域池化缺框时 `region_mean` 恒为 0，δ 无方差，AUC 结构性地恒等于 0.500。

**修正**：`e1_validity.py` 加**退化守卫**——投影值全为常数即报"不可估（该口径在该缓存里结构性缺失）"，
并记录原因，**不输出任何 AUC 数字**。同时补上一条 `vision_mean` 口径的冻结方向
（`v_hazard_clean_vision.npy`，$L^*$ = 21），使 tier_m 这一格至少有一个可算的 G 轴方向。

**连带发现（已写入报告 §5.1 Table E1b）**：`v_hazard_clean_vision` 在 tier_m 的 A vs D 上得 0.673，
但 tier_m 那一轮**没有几何匹配负例**（缺 bbox 就无法做 caliper 匹配）；
同一条方向在 n1_d2 的**几何匹配** D 上只有 0.517。差值 +0.156 即几何混淆的幅度。
因此红线 1 的"独立挖掘轮"一格判**不可估**，不判通过。

## DV/A21 Stage D 逐层随机零分布的完整跑数记录（收口）

按修正案 A3，随机方向对照必须**逐层自带零分布**、比**整条 ±α 阶梯的斜率**。本轮共跑完 7 组：

| 层 / token 集 | seed 数 | 零分布 mean ± sd | 备注 |
| --- | --- | --- | --- |
| L8 / vision | 20 | +0.0015 ± 0.0146 | 本轮新跑 |
| L9 / vision | 20 | −0.0009 ± 0.0128 | 本轮新跑 |
| L18 / vision | 20 | +0.0003 ± 0.0026 | 本轮新跑 |
| L22 / vision | 20 | +0.0004 ± 0.0012 | 本轮新跑 |
| L23 / query | 20 | −0.0166 ± 0.0642 | 本轮新跑 |
| L10 / query | 15 | −0.0056 ± 0.0136 | 沿用既有 `t2_random_null_lang_ext.json` |
| L22 / query | 15 | −0.0052 ± 0.0269 | 沿用既有 `t2_random_null_brake_ext.json` |

**结论**：11 条待测方向**无一超出**其同层零分布（经验 $p$ 最小 0.125），
包括按构造必然有效、且已被解析 Jacobian 独立确认的 $v_{brake}$（$z$ = −1.32，$p$ = 0.188），
以及效应最大的 $v_{hazard}^{clean}$@L23（斜率 −0.0743，为 $v_{brake}$ 的 1.8 倍，$z$ = −0.90，$p$ = 0.333）。
故本轮**全部 E2 判定为"不可估"**，且该判定的归因是明确的**仪器侧**：
零分布离散度（sd 0.0012 ~ 0.0642，跨层相差 50 倍以上）与待测效应同量级或更大。

**这条记录本身即是 A3 的定量确证**：query 位置上零分布 sd 随层单调增长
（L10 0.0136 → L22 0.0269 → L23 0.0642），任何跨层套用同一判据的做法都会在浅层过度宽松、
在深层过度严格。要让 E2 从"不可估"变为可判，须按 `final_report.md` §C.1 已登记的方向改造对照
（从激活流形内采样真实激活两两之差，替代各向同性高斯），而不是简单加大 seed 数。

---

# 第三轮：三线头号证据 + pilot post-train（HL）

## HL/A22 轮询脚本自匹配 bug（用户发现，记入自我更正记录）

**现象**：本轮用 `until ! pgrep -f "<pattern>" >/dev/null; do sleep N; done` 等待后台 GPU 作业退出时，
两个等待任务永远不返回。GPU 早已空闲、被等的计算早已完成并落盘。

**根因**：`pgrep -f` 匹配的是**整条命令行**，而轮询循环自身的命令行里就包含那个 pattern 字符串
（`bash -c 'until ! pgrep -f "scripts/t2_steer.py.*dvnull" ...'`），于是 pgrep 匹配到轮询进程自己 ⇒
条件恒为真 ⇒ 死等。同一个坑还出现在 `pgrep -f "tag _dvq_cand1_L22"` 那一次。

**代价**：没有丢失任何计算，也没有产生错误结果，**只浪费了几个小时的等待**。
两个僵死的等待任务已确认杀掉。

**下次的做法（三选一，按可靠性排序）**：
1. **等 PID 而不是等模式**：`until [ ! -e /proc/<PID> ]; do sleep N; done`（本轮后半程改用此法，有效）；
2. **等产出物而不是等进程**：`until [ -f <output.json> ]; do sleep N; done`（最稳，且顺带确认作业真的成功）；
3. 必须用 pgrep 时，**加进程名过滤并排除自身**：
   `pgrep -f -x ... ` 或 `pgrep --ns $$ -f ... | grep -v $$`，或让 pattern 不出现在轮询命令行里
   （把 pattern 写进一个变量文件，轮询脚本读文件）。
**一般化教训**：任何"按命令行文本搜索进程"的轮询，都必须先问一句"这个 pattern 会不会匹配到我自己"。

## HL/A23 新工单文件缺失，按用户消息中的规格执行（重要，请回来核对）

**问题**：用户指示执行 `docs/headline_three_line_pilot_workorder.md`，但**该文件不在磁盘上**
（`docs/` 最后修改于 2026-08-29 00:38，即上一轮四份文档同步的时刻；全盘检索无同名或近似文件）。
同步很可能没有真正落地。

**处理**：用户已下线并要求自主执行到底，且其消息本身给出了足以执行的规格。故**按用户消息中的规格执行**，
不阻塞等待。本轮据以执行的规格逐条抄录如下（供用户回来与原工单比对）：

1. **补齐轴矩阵缺口**：F 轴补测 DiffusionDrive，C 轴补测 SimLingo，使两模型的 G/F/I/C 矩阵对齐；
2. **pilot post-train**（针对 SimLingo）：用已确证的 F 轴诊断（驱动动作的方向与编码危险的方向近乎正交）
   设计一个**耦合辅助损失**去修；前后对比**行为分**与**该余弦值本身**；
   **不能只看行为分变好就默认诊断被验证，要检验耦合是否真的被改变**；
3. **三线头号证据表**：公开榜单排名 vs 四轴矩阵 vs 真实 post-train 结果；**不做任意加权的单一复合分**；
4. **公开榜单分数**（用户提供）：SimLingo CARLA LB2.0 driving score **6.87**；
   DiffusionDrive NAVSIM PDMS **88.1**。**两者不在同一基准上，不能直接比大小；
   这一点必须作为论据写出来，不是当限制带过**；
5. **输出**：每个子任务中英双语、期刊 Methods/Results/Discussion 结构、Results 用规范三线表；
6. **最终交付物**：`results/paper_experiments_section_{zh,en}.md` —— 按标准 CVPR/期刊 Experiments 章节
   结构（Setup / Main Results / Ablation-like 分析 / Discussion of Limitations）重新组织整条工作线，
   突出"公开榜单排名与真实部署排名可能脱节，G/F/I/C 四轴定位脱节发生在因果链哪一环"这条主线；
7. **完成标志**：`results/headline_pilot_DONE.md`，写明所有产出物路径与一句话总览。

**风险声明**：原工单里若含本消息未覆盖的设计细节（如指定的 pilot 训练预算、指定的行为分定义、
指定的对照臂），本轮的相应决策是我自行按项目一贯纪律做的，已逐条记入各报告的"自我更正记录"。

## HL/A24 C 轴 top-2 占比公式的适用性判据（SimLingo 上前提不成立）

**发现**：给 SimLingo 补测 C 轴后，其逐层恢复剖面是**从 L0 单调递减到 L23**
（1.004 → 0.000，Spearman(层号, recovery) = **−0.997**, p = 1.3e-26），
而 DiffusionDrive 的剖面是**内部峰**（L0/L1 近 0，峰在 L6，Spearman **+0.881**）。

协议 §3⑤ 的 $C_m$ = top-2 层 recovery 占比，其**隐含前提是剖面存在内部峰**。
剖面单调递减时（"级联"签名：失效在最前端就已进入、patch 越早恢复越多），
top-2 占比量到的只是"1/L 的某个倍数"，**不构成集中度证据**。
SimLingo 的 $C_m$ = 0.176（弥散基线 0.083）若照字面读会得到"PASS：失效集中"，**这是误读**。

**修正**：新增 `scripts/c_axis_shape.py`，对两个模型统一计算三个与层数无关的形状量并写回各自 JSON：
① Spearman(层号, recovery)；② 达到 80% 恢复质量所需层数占比；③ 责任层 argmax 的归一化熵。
并加**适用性判据**：ρ < −0.7 判定为级联 ⇒ top-2 公式不适用，C 判 **不可估（公式前提不成立）**，
实质结论改述为「失效在视觉输入接口即已进入并向下游级联」。
DiffusionDrive（ρ = +0.881）不受影响，仍判 PASS。

**连带纪律**：$C_m$ 的弥散基线随层数变化（8 层 0.250 vs 24 层 0.083），
**跨模型不可直接比较 $C_m$ 数值**；可比的是上述三个形状量。已写入三线表的可比性列。

## HL/A25 DiffusionDrive 的 F 轴 steering 仪器无分辨力（含站内上界标定与剂量升级）

**观测**：在 DiffusionDrive 自己的概念峰层 L5 注入 G 轴方向 $v_{hazard}^{dd}$，
±α ∈ {0.5,1,2,4} 的全阶梯斜率为 **−0.00000 [−0.00013, +0.00008]** m/s per σ，
与同层 20 seed 随机零分布不可区分（z = −0.01）。

**这是仪器失效还是标本属性？两条排查，都指向仪器侧**：
1. **注入确实到达了模型**：横向偏移随 α **单调且对称**变化（α=+0.5→+4：−0.0002 → −0.0022 m；
   负向镜像），舒适度亦然。故不是钩子/返回值没生效。
2. **站内上界标定失败**：另造 $v_{brake}^{dd}$（用 DiffusionDrive **自身** commanded_speed 对 ego 速度
   回归后的残差二分作标签，逐层判别，held-out AUC 0.656 @L5，与 SimLingo 的 $v_{brake}$ 同构）。
   按构造它必然与行为相关，但注入它的全阶梯斜率仍只有 **+0.00002 [−0.00013, +0.00021]**，
   同样未超随机零分布。**连按构造必然有效的轴都推不动 ⇒ 该仪器在此标本上无分辨力。**
3. **剂量升级到 ±32σ 仍无效**：α ∈ {4,8,16,32} 下 Δv 最大仅 **−0.0020 [−0.0123, +0.0055]** m/s，
   而同一注入下横向已达 −0.0203 m（α=+32 时 |Δ横向|/|Δv| = 9.96）。不存在阈值效应。

**结论与影响**：DiffusionDrive 的纵向规划输出由 anchored 扩散头（20 个 k-means 轨迹锚）主导，
表征层扰动在测试剂量内不改变锚的选择 ⇒ **F 轴以 steering 效应量操作化时，在两类动作头之间不同量纲，
不得同表排序**。这不是"通路断裂"的证据，而是"该操作化方式不可移植"的证据。
故三线表中 F 轴的 steering 行标注为**不可比**，F 的跨模型对齐改用协议 §3.5 指定的
**行动层反事实测试**（读出对象是动作、不依赖动作头形式，架构中立），两模型均已跑。

## HL/A26 pilot post-train 的设计决策与仪器侧限定（原工单缺失，以下为自行决策）

原工单文件不在磁盘（见 HL/A23），以下设计由我按项目一贯纪律自行决定，逐条登记：

**① 训练预算：只训 `speed_wps_head`（229k 参数），其余全部冻结。**
理由不是省算力，而是这**恰好是选型协议 §1 核心假设的直接检验**——
"内部已编码危险的候选，post-train 只需把读出接到动作上 ⇒ 便宜"。
头之前全部冻结 ⇒ 每个样本的输入特征是常量 ⇒ 一遍前向缓存后离线训练，
整个 pilot 的 GPU 成本 = 2296 次前向。

**② 耦合项的形式：$\\lambda\\cdot\\mathrm{ReLU}(\\Delta_{1\\sigma} + m)$，其中
$\\Delta_{1\\sigma} = v_{cmd}(f + 1\\sigma\\hat v_{hazard}) - v_{cmd}(f)$，margin $m$ = 0.05 m/s。**
关键选择：$\\Delta_{1\\sigma}$ **就是 steering 实验测的 α=+1 剂量响应本身**，不是另造的代理量 ——
"修的量"与"验的量"必须同源，否则"耦合被改变"无法与 steering 读数对齐。

**③ 三臂而非两臂**：必须有 **A1（λ=0，纯任务微调）** 这条归因对照。
事后证明这条对照是决定性的：A2−A0 的行为分增量 = **+0.051**，看起来像成功；
但 A1−A0 = **+0.050**，A2−A1 = **+0.001 [−0.004, +0.007]** ⇒
**行为分的改善几乎全部来自任务微调，与耦合项无关**。
若只跑 A0/A2 两臂，本轮会得出"诊断被验证"的错误结论——这正是用户预先点名要防的误判。

**④ 行为分用三个并列读数，不用单一指标**：
b-AUC(A vs D2a)（held-out scene）、MAE(v_cmd, 人类速度)、ρ(b, 人类真实 a_brake)。
三者在 A1 与 A2 之间**全部持平**（0.592/0.593；0.671/0.673；+0.160/+0.162），结论不依赖指标选择。

**⑤ 仪器侧限定（必须与结论并列陈述）**：耦合所指向的方向 $v_{hazard}$@L23
在 held-out（ghost 条件，n=628）上对真实 $a_{brake}$ 残差的 ρ = **+0.053（p = 0.183）**——
符号正确但**未达显著**。α 由 train 内 scene 级 GroupKFold 选定（α=100，train CV r = +0.008），
未看 held-out。故本 pilot 的准确表述是：
**"把动作耦合到一条只在弱信号意义上读出危险的方向上，耦合量被大幅改变，但行为分没有增量收益。"**
不能表述为"把动作接到危险读出上没用"——后者要求耦合目标本身先通过显著性检验。
首版曾用 pooled（clean+ghost）样本、固定 α=1e3 拟合该方向，held-out ρ = −0.028（**符号还是反的**）；
改为只用 ghost 条件（与 T-F 预注册条件一致）+ train 内 GroupKFold 选 α 后，符号转正。
两版数字均落盘于 `p2_pilot_posttrain.json` 的运行记录，此处如实登记该次修正。

**⑥ 一条附带发现**：纯任务微调（A1）**本身就把耦合量改动了一部分**
（Δcos = −0.061，占 A2 全部改动 −0.113 的 54%）——即"照着人类真实减速去拟合动作头"
会自动带来部分通路连接。耦合项把它再推远一倍（−0.113），**却没有换来任何额外行为收益**。
⇒ **耦合强度与行为分是可解离的**：在本预算下，F 轴耦合不是该行为指标的约束瓶颈。
