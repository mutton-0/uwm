# 完成标志：F-3 多帧候选遮挡漏洞修复

> 工单：[`../docs/f3_multiframe_occlusion_fix_workorder.md`](../../docs/f3_multiframe_occlusion_fix_workorder.md)。
> 完成日期：2026-09-02。修正案：[`amendments.md`](amendments.md) §FM/A56–A58（累计 58 条）。
> 详细报告：`results/f3_multiframe_fix_report_{zh,en}.md`。

---

## 1. 直接结论

| 问题 | 答案 |
| --- | --- |
| 漏洞真实存在吗？ | **是，且两个候选都受影响**（工单只确认了 Alpamayo） |
| 漏了多少？ | Alpamayo **396/396 帧（100%）** 本该遮而未遮；AutoVLA **409/476（86%）** |
| 修复后判定变了吗？ | **没变。** 两个候选都仍是「不可估：基线响应本身与 0 不可区分」 |
| 白修了吗？ | **没有。** 判定门是 $b_{ghost}$（未遮挡臂），修复**按构造不可能**改变它；这是"当前样本恰好不暴露"的正确性缺陷，不是无害 |

**工单对 AutoVLA 的估计被实测更正**（§FM/A57）：工单按 `t_emergence` 推断 AutoVLA 不受影响（0%），
逐帧投影实测是 **86% 的窗口帧仍能看见实体**。原因是 `t_emergence` 标记的是
"进入走廊 / TTC 越阈"，**不是"变得可见"**——行人在进入走廊前往往已在人行道上清晰可见。
**"早于 emergence" ≠ "看不见"**，已升为纪律。

## 2. 修复内容

`scripts/f3_window_boxes.py`（新增）+ `scripts/f3_occlusion_vla.py`（改）：
对窗口内**每个时间戳**独立投影实体、可见则用**该帧自己的**均值色遮挡。

* 投影**复用挖掘期的 `compute_scene_geometry` + `frame_bbox`，一行未改**；
* 时间戳 → 网格下标是**精确匹配**（两个 VLA 吃的都是真实 nuScenes CAM_FRONT 图像），
  偏差 > 60 ms 判失败并计数（实测为 0）；
* 某帧确实不可见就不动，**不外推、不近似**，计入 `frames_no_box`；
* **对照臂逐帧涂同一对照框**，保证遮挡帧数在两臂间可比；
* 新增守卫：窗口内一帧都没投影出实体的事件剔除并计数（实测两候选均为 0）；
* Alpamayo 的两路前视（120° / 30°）逐帧都遮。

## 3. 新旧对比（G1 语料，事件数与旧版一致）

| 候选 | 版本 | $b_{ghost}$ | $R$ | 判定 |
| --- | --- | --- | --- | --- |
| Alpamayo-R1 (n=99) | 旧 | +0.0328 [−0.1228, +0.1907] | +0.028 [−0.127, +0.180] | 不可估（无基线响应） |
| Alpamayo-R1 (n=99) | **新** | +0.0328 [−0.1228, +0.1907] | **−0.033 [−0.290, +0.188]** | **不可估（无基线响应）** |
| AutoVLA (n=119) | 旧 | −0.0551 [−0.2194, +0.0277] | −0.590 [−1.607, +0.033] | 不可估（无基线响应） |
| AutoVLA (n=119) | **新** | −0.0558 [−0.2206, +0.0275] | **−0.433 [−1.464, +0.193]** | **不可估（无基线响应）** |

**修复确实改动了遮挡臂，且远超运行间噪声**（$b_{ghost}$ 按构造不受修复影响，其非零量即噪声地板）：

| 候选 | Δ$b_{ghost}$（噪声地板） | Δ$b_{occ}$（修复效应） | 比值 |
| --- | --- | --- | --- |
| Alpamayo-R1 | 0/99 非零，均值 0.0000 | **99/99 非零，均值 0.1144** | ∞ |
| AutoVLA | 3/119 非零，均值 0.0012 | 24/119 非零，均值 0.0196 | **16.3×** |

## 4. 顺带核查（工单 §1.5、§0 末段）

| 核查项 | 结论 |
| --- | --- |
| 单帧候选的 `spd(ev)` 是否泄漏？ | **否**：标量、取自 clean 侧、三臂共用同一值，不含视觉信息 |
| 单帧候选的图像通道？ | **否**：只吃一张图，遮挡即完全移除，无"历史帧"通道 |
| **DiffusionDriveV2 的 lidar 通道？** | **是，同性质缺陷，未修**：遮挡只涂 RGB，DDv2 同时吃真实点云 ⇒ **行人在点云里仍完整存在**。当前 DDv2 判定同为"无基线响应"、卡在 $b_{ghost}$，故结论不受影响，但已登记（§FM/A58），列入未完成项 |
| `x_{cond}_frames[0]` 总是存在？ | **是**（288 个 A 类事件全有，且带 bbox） |
| 总是可用？ | **否**：Alpamayo 的 `covers()` 预筛 288 → 186。**"这一帧不可用"是真实情况**，现有跳过+计数纪律正确 |
| 窗口终点取 $t_0$ 合理？ | **是**：两个适配器的窗口本就以目标帧结尾，与模型真实输入一致，非我们强加 |

## 5. 产出物

| 路径 | 说明 |
| --- | --- |
| `scripts/f3_window_boxes.py` | **新增**：逐帧投影器（复用挖掘期投影，带匹配容差与失败计数） |
| `scripts/f3_occlusion_vla.py` | **改**：四臂改为逐帧遮挡；新增 `n_frames_occluded` / `window_projection_stats` / `occ_no_frame_hit` 守卫 |
| `results/f3_occlusion_{alpa,autovla}_mf.json` | 修复后读数（**旧文件 `f3_occlusion_{alpa,autovla}.json` 保留不覆盖**，供逐格对照） |
| `results/f3_multiframe_fix_report_{zh,en}.md` | 中英双语报告 |

**旧结果文件一个没删**：修复版另存 `_mf` 后缀，新旧可逐格对照。

## 6. 未完成项

| 项 | 状态 | 理由 |
| --- | --- | --- |
| **DDv2 的 lidar 通道遮挡** | 未修 | 同性质缺陷，正确做法是同时删掉落在 3D 框内的点云点。本工单范围只覆盖多帧遮挡 |
| 前车急刹 / NAVSIM 上重跑 | 未做 | 工单 §2.3 明确不要求。注意两个 VLA 在 NAVSIM 上本就是 n/a |
| 多帧多次推理取平均降噪 | 未做 | 工单 §2.3 明确留给下一个工单。AutoVLA 的噪声地板已量化（均值 0.0012）可作输入 |
| 全量（去掉 `--limit`） | 未做 | 沿用旧版 limit 以保证新旧同口径可比 |
