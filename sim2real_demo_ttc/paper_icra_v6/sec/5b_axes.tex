% !TEX root = ../main.tex
\section{Diagnosis: Reading the Two Axes}
\label{sec:axes}

The diagnosis reads a policy on the two axes of \eqref{eq:FI}. We apply both interventions to the
\NumSGscenes{} NAVSIM Singapore near-pedestrian scenes and read, per scene, $F$, the plan
displacement caused by removing the target pedestrian, and $I$, that caused by the night-style
perturbation.

\begin{figure}[t]
\centering
\includegraphics[width=\columnwidth]{figures/cases.pdf}
\caption{\textbf{Four right-hand-drive cases, and displacement against the separation it buys.}
(a--d)~Ego at the origin, the vulnerable road user's (VRU) logged future dashed from the star, and DiffusionDriveV2's own BEV
semantic head as the background; every policy receives the frame's logged navigation, so the turns are
commanded. (a)~Clearances
$0.3$--$0.9$\,m. (b)~SimLingo passes at $3.2$\,m, the other five at $6.7$--$10$\,m. (c,~d)~The departing policies and their planned distances are named in the panel. (e)~Every policy--scene cell of the
\NumSGscenes{} Singapore scenes: $F$ against $\Delta S$; band: the 10--90\% spread of $\Delta S$ per
$F$ bin; dashed line: $F=0.5$\,m.}
\label{fig:cases}
\end{figure}

\paragraph{When the plan does move, which way does it move?}
$F$ measures how far a plan moves, not where it moves to. We therefore decompose it (\cref{fig:cases}e), reading $F$ against $I$ of
\eqref{eq:FI}---the same policy's jitter floor under an unrelated edit---and against $\Delta S$, the
change in clearance to the pedestrian's logged future. A response counts as avoidance only if all three hold: the plan moved
($F\ge0.5$\,m), it moved more than the policy's own jitter ($F>I$), and the movement bought distance
($\Delta S\ge0.5$\,m).

Three readings follow. First, the pedestrian outweighs the irrelevant edit in only
\NumFgtIlo--\NumFgtIhi\% of scenes, so most of what $F$ registers sits inside the policy's own
variability---the pinched band of \cref{fig:cases}e. Second, avoidance is rare where it is called for. The logged ego is
stationary through most of this corpus, so a plan that does not move is usually the correct one; we
therefore replay every scene at input speeds of 4 and 8\,m/s, as in \cref{sec:checkup}, and read only the
cells in which the pedestrian-blind plan does come within 1\,m of the pedestrian's logged future.
Across \NumCfModels{} policies that is \NumCfNeed{} of \NumCfCells{} cells (\NumCfNeedLo--\NumCfNeedHi\%
per policy), and \NumCfReal{} of them---\NumCfRealPct\%---are genuine avoidance. On those cells the
median clearance changes by \NumCfDsLo--\NumCfDsHi\,m and the median planned distance by
\NumCfArcLo--\NumCfArcHi\,m: facing a pedestrian it would otherwise reach, no policy's plan diverts or slows under our open-loop protocol. Third, the displacement that does occur is not aimed: of the
\NumMoveN{} cells in which the plan moved, \NumMoveFar{} increased the clearance and \NumMoveNear{}
decreased it. Per policy the split differs sharply: DiffusionDrive reacts in few
scenes but in the right direction (median $\Delta S=\NumDDmed$\,m), while SimLingo moves in more
scenes than the other five combined and moves \emph{towards} the pedestrian more often than away
(\NumSLnear{} against \NumSLfar, median $\NumSLmed$\,m); it also has the highest rate of sub-1.5\,s time-to-proximity (\cref{tab:bench}). SimLingo is trained in simulation and run here on real images, so part of what we measure is a sim-to-real gap, which is exactly what a check-up at a new site should surface. Finally, what a policy yields does not grow with the hazard: the
rank correlation between $\Delta S$ and the deceleration the scene demands is
\NumRhoLo--\NumRhoHi{}.

An exam that rewarded responsiveness alone would therefore rank first the policy with the most sub-1.5\,s encounters. A score merges these three readings into one number; the edited frames keep them apart.
